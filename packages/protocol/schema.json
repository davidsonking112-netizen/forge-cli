{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://forge-cli.dev/protocol/v1/event.schema.json",
  "title": "Forge protocol event",
  "type": "object",
  "required": ["protocol", "id", "sessionId", "type", "timestamp"],
  "properties": {
    "protocol": { "const": 1 },
    "id": { "type": "string", "minLength": 1 },
    "sessionId": { "type": "string", "minLength": 1 },
    "type": { "type": "string", "minLength": 1 },
    "timestamp": { "type": "string", "format": "date-time" }
  },
  "allOf": [
    {
      "if": { "properties": { "type": { "const": "agent.text" } } },
      "then": {
        "required": ["text"],
        "properties": { "text": { "type": "string" } }
      }
    },
    {
      "if": { "properties": { "type": { "const": "agent.scratchpad" } } },
      "then": {
        "required": ["items"],
        "properties": {
          "items": {
            "type": "array",
            "maxItems": 64,
            "items": {
              "type": "object",
              "required": ["key", "value", "status"],
              "properties": {
                "key": { "type": "string", "maxLength": 100 },
                "value": { "type": "string", "maxLength": 1000 },
                "status": { "enum": ["todo", "active", "done", "blocked"] }
              },
              "additionalProperties": false
            }
          }
        }
      }
    },
    {
      "if": { "properties": { "type": { "const": "agent.checklist" } } },
      "then": {
        "required": ["items"],
        "properties": {
          "items": {
            "type": "array",
            "maxItems": 64,
            "items": {
              "type": "object",
              "required": ["id", "label", "expectation", "status"],
              "properties": {
                "id": { "type": "string", "maxLength": 100 },
                "label": { "type": "string", "maxLength": 300 },
                "expectation": { "type": "string", "maxLength": 500 },
                "status": {
                  "enum": ["pending", "active", "complete", "blocked"]
                },
                "note": { "type": "string", "maxLength": 500 }
              },
              "additionalProperties": false
            }
          }
        }
      }
    },
    {
      "if": { "properties": { "type": { "const": "agent.delegation" } } },
      "then": {
        "required": ["role", "status", "turns", "text"],
        "properties": {
          "role": { "enum": ["explorer", "implementer", "tester", "reviewer"] },
          "status": { "enum": ["completed", "failed", "skipped"] },
          "turns": { "type": "integer", "minimum": 0 },
          "text": { "type": "string" },
          "budget": {
            "type": "object",
            "required": [
              "profile",
              "plannedRoles",
              "usedRoles",
              "plannedTurns",
              "usedTurns",
              "contextChars",
              "outputChars",
              "skippedRoles"
            ],
            "properties": {
              "profile": { "enum": ["economy", "balanced", "quality"] },
              "plannedRoles": { "type": "integer", "minimum": 0, "maximum": 4 },
              "usedRoles": { "type": "integer", "minimum": 0, "maximum": 4 },
              "plannedTurns": {
                "type": "integer",
                "minimum": 0,
                "maximum": 32
              },
              "usedTurns": { "type": "integer", "minimum": 0, "maximum": 32 },
              "contextChars": {
                "type": "integer",
                "minimum": 0,
                "maximum": 100000
              },
              "outputChars": {
                "type": "integer",
                "minimum": 0,
                "maximum": 100000
              },
              "skippedRoles": {
                "type": "array",
                "maxItems": 4,
                "items": { "type": "string", "maxLength": 30 }
              }
            },
            "additionalProperties": false
          }
        }
      }
    },
    {
      "if": { "properties": { "type": { "const": "agent.repair" } } },
      "then": {
        "required": ["attempt", "maxAttempts", "strategy", "status", "reason"],
        "properties": {
          "attempt": { "type": "integer", "minimum": 1, "maximum": 4 },
          "maxAttempts": { "const": 4 },
          "strategy": { "enum": ["alternate", "deep-thinking"] },
          "status": { "enum": ["started", "succeeded", "failed", "exhausted"] },
          "reason": { "type": "string", "maxLength": 1000 }
        },
        "additionalProperties": false
      }
    },
    {
      "if": { "properties": { "type": { "const": "agent.state" } } },
      "then": {
        "required": [
          "phase",
          "status",
          "stepIndex",
          "totalSteps",
          "artifact",
          "artifactId",
          "entryConditions",
          "requiredArtifact",
          "exitCondition",
          "failureTransition",
          "note",
          "budget"
        ],
        "properties": {
          "phase": {
            "enum": [
              "intake",
              "inspect",
              "plan",
              "milestone",
              "implement",
              "targeted-verify",
              "evidence",
              "repair",
              "full-verify",
              "summarize"
            ]
          },
          "status": { "enum": ["active", "blocked", "completed", "failed"] },
          "stepId": { "type": "string", "maxLength": 100 },
          "stepIndex": { "type": "integer", "minimum": 0, "maximum": 64 },
          "totalSteps": { "type": "integer", "minimum": 1, "maximum": 64 },
          "artifact": { "type": "string", "maxLength": 100 },
          "artifactId": { "type": "string", "maxLength": 100 },
          "entryConditions": {
            "type": "array",
            "maxItems": 8,
            "items": { "type": "string", "maxLength": 500 }
          },
          "requiredArtifact": { "type": "string", "maxLength": 1000 },
          "exitCondition": { "type": "string", "maxLength": 1000 },
          "failureTransition": { "type": "string", "maxLength": 1000 },
          "note": { "type": "string", "maxLength": 1000 },
          "budget": {
            "type": "object",
            "required": [
              "providerTurns",
              "maxProviderTurns",
              "toolCalls",
              "maxToolCalls",
              "repairAttempts",
              "maxRepairAttempts"
            ],
            "properties": {
              "providerTurns": {
                "type": "integer",
                "minimum": 0,
                "maximum": 128
              },
              "maxProviderTurns": {
                "type": "integer",
                "minimum": 1,
                "maximum": 128
              },
              "toolCalls": { "type": "integer", "minimum": 0, "maximum": 256 },
              "maxToolCalls": {
                "type": "integer",
                "minimum": 1,
                "maximum": 256
              },
              "repairAttempts": {
                "type": "integer",
                "minimum": 0,
                "maximum": 4
              },
              "maxRepairAttempts": {
                "type": "integer",
                "minimum": 1,
                "maximum": 4
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      }
    },
    {
      "if": { "properties": { "type": { "const": "session.start" } } },
      "then": {
        "properties": {
          "profile": { "type": "string", "minLength": 1 },
          "workspaceFingerprint": {
            "type": "string",
            "pattern": "^[a-f0-9]{64}$"
          },
          "recovery": {
            "type": "object",
            "required": [
              "sourceSessionId",
              "decision",
              "reasonCode",
              "reason",
              "workspaceChanged",
              "nextAction"
            ],
            "properties": {
              "sourceSessionId": { "type": "string", "minLength": 1 },
              "decision": {
                "enum": ["continue", "re-plan", "manual-intervention"]
              },
              "reasonCode": {
                "enum": [
                  "unchanged-active-step",
                  "unchanged-no-active-step",
                  "completed-session",
                  "legacy-session",
                  "workspace-drift",
                  "workspace-missing"
                ]
              },
              "stepId": { "type": "string", "minLength": 1 },
              "reason": { "type": "string", "maxLength": 500 },
              "workspaceChanged": { "type": "boolean" },
              "nextAction": {
                "enum": ["resume", "re-plan", "inspect-workspace"]
              }
            },

            "additionalProperties": false
          }
        }
      }
    },
    {
      "if": { "properties": { "type": { "const": "agent.plan" } } },
      "then": {
        "required": ["goal", "steps", "assumptions", "verification"],
        "properties": {
          "goal": { "type": "string" },
          "steps": { "type": "array" },
          "assumptions": { "type": "array" },
          "verification": { "type": "array" }
        }
      }
    },
    {
      "if": { "properties": { "type": { "const": "tool.proposal" } } },
      "then": {
        "required": ["tool", "risk", "arguments", "reason"],
        "properties": {
          "tool": { "type": "string" },
          "risk": { "type": "string" },
          "arguments": { "type": "object" },
          "reason": { "type": "string" }
        }
      }
    },
    {
      "if": { "properties": { "type": { "const": "tool.result" } } },
      "then": {
        "required": ["tool", "ok", "approved", "durationMs"],
        "properties": {
          "tool": { "type": "string" },
          "ok": { "type": "boolean" },
          "approved": { "type": "boolean" },
          "durationMs": { "type": "number", "minimum": 0 }
        }
      }
    },
    {
      "if": { "properties": { "type": { "const": "approval.result" } } },
      "then": {
        "required": ["proposalId", "decision"],
        "properties": {
          "proposalId": { "type": "string", "minLength": 1 },
          "decision": {
            "enum": ["approve-once", "approve-session", "deny", "cancel"]
          },
          "category": { "enum": ["automatic", "user", "policy"] },
          "scope": {
            "type": "object",
            "required": ["tool", "argumentDigest", "summary", "expiresAt"],
            "properties": {
              "tool": { "type": "string", "minLength": 1 },
              "argumentDigest": {
                "type": "string",
                "pattern": "^[a-f0-9]{64}$"
              },
              "summary": { "type": "string", "maxLength": 200 },
              "expiresAt": { "type": "string", "format": "date-time" },
              "paths": {
                "type": "array",
                "maxItems": 100,
                "items": { "type": "string", "maxLength": 500 }
              }
            },
            "additionalProperties": false
          }
        }
      }
    },
    {
      "if": { "properties": { "type": { "const": "session.complete" } } },
      "then": {
        "required": ["status", "summary", "changedFiles", "checks"],
        "properties": {
          "status": { "enum": ["completed", "failed", "cancelled"] },
          "summary": { "type": "string" },
          "changedFiles": { "type": "array", "items": { "type": "string" } },
          "checks": {
            "type": "array",
            "maxItems": 32,
            "items": {
              "type": "object",
              "required": ["command", "ok", "exitCode", "output"],
              "properties": {
                "command": { "type": "string", "maxLength": 500 },
                "ok": { "type": "boolean" },
                "exitCode": { "type": ["integer", "null"] },
                "output": { "type": "string", "maxLength": 100000 },
                "status": {
                  "enum": [
                    "not-run",
                    "passed",
                    "failed",
                    "timed-out",
                    "blocked",
                    "stale"
                  ]
                },
                "startedAt": { "type": "string", "format": "date-time" },
                "finishedAt": { "type": "string", "format": "date-time" },
                "workspaceFingerprint": {
                  "type": "string",
                  "pattern": "^[a-f0-9]{64}$"
                },
                "commandDigest": {
                  "type": "string",
                  "pattern": "^[a-f0-9]{64}$"
                },
                "toolVersion": { "type": "string", "maxLength": 32 },
                "outputTruncated": { "type": "boolean" }
              },
              "additionalProperties": false
            }
          }
        }
      }
    }
  ]
}
